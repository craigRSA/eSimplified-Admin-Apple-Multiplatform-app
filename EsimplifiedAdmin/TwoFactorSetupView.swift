import SwiftUI
import EsimplifiedKit

// Stub — implemented in Task 7 (TOTP enrollment with QR).
struct TwoFactorSetupView: View {
    let host: String
    let accessToken: String

    var body: some View {
        Text("2FA setup — coming in Task 7")
            .padding()
    }
}
