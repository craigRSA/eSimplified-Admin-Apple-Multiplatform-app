import SwiftUI
import EsimplifiedKit

// Stub — implemented in Task 6 (login + TOTP challenge).
struct LoginView: View {
    @Bindable var model: AdminAppModel

    var body: some View {
        Text("Login — coming in Task 6")
            .padding()
    }
}
